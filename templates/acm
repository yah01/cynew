#include <bits/stdc++.h>

typedef long long ll;
typedef std::pair<int, int> pii;
const int maxn = 1e5 + 5, INF = (1 << 30) - 1;
const int mod = 1e9 + 7;

const double PI = acos(-1.0);

using namespace std;

int main()
{
    //code here..

    return 0;
}